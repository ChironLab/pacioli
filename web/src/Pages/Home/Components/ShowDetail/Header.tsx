import React from 'react';

type Props = {
  description: string | null,
  meta: any | null
}

const Header = ({ description, meta }: Props) => {
  const metaKeys = meta && Object.keys(meta)

  




  return <div> yaya </div>;
};

export default Header;
